# En esta prueba se va a comprobar la funcionalidad del bucle while.
# El bucle debe tener dos "goto", el primero irá después de la expresión,
# indicando si se cumple a que línea se debe ir. El otro en la línea siguiente,
# indicando si no se cumple la expresión a donde debe ir.
# Después de las sentencias que haya dentre las llaves del bucle, habrá otro
# "goto" que llevará a la expresión que se debe evaluar de nuevo para ver si el bucle
# continua o no.

prog ejemplo2 {
    proc sumar (int resul:in out) {
        int aux;
        aux = x;
        while aux /= 0 loop {
            iteraciones = iteraciones + 1;
        }
        finally {
            println(iteraciones);
        }; # fin del while
    }
}
